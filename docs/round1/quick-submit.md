# Quick Submit

```bash
cd ~/Desktop/Projects/Forecast-Audit
source .venv/bin/activate
cp .env.example .env
pip install -r requirements.txt
python validate.py
```

Then use the Docker and Hugging Face steps in `docs/round1/submission-guide.md`.
