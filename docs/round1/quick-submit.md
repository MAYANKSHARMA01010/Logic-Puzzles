# Quick Submit

```bash
cd ~/Desktop/Projects/Logic-Puzzles
source .venv/bin/activate
cp .env.example .env
pip install -r requirements.txt
python validate.py
```

Then use the Docker and Hugging Face steps in `docs/submission.md`.
